import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

export const LogoutScreen = async (value) => {
    try {
      
    //   await AsyncStorage.setItem('auth_token', value);
    } catch (e) {
      // saving error
    }
  }
