import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

export const LogoutScreen = async (value) => {
    try {
        console.log('slog out')
    //   await AsyncStorage.setItem('auth_token', value);
    } catch (e) {
      // saving error
    }
  }
